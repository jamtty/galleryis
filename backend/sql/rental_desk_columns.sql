-- ---------------------------------------------------------------------------
-- 대관 신청 — 대리 신청(전화 · 방문 접수) 입력자
--
-- 관리자 일정(/admin/schedule)에서 신청가능 칸을 눌러 직원이 대신 접수한
-- 신청서에 "누가 입력했는지" 를 남깁니다. (공개 신청서로 들어온 건 비어 있습니다)
-- 신청서 수정 화면이 이 값을 보여 줍니다.
--
-- phpMyAdmin 또는 콘솔에서 1회 실행하세요. (이미 있으면 오류가 나도 무시)
-- ---------------------------------------------------------------------------

ALTER TABLE `g4_write_order`
  ADD COLUMN `rt_created_by` varchar(64) NOT NULL DEFAULT '' COMMENT '대리 신청 입력자(관리자 id)' AFTER `rt_terms_at`;

-- 확인
SHOW COLUMNS FROM `g4_write_order` LIKE 'rt_created_by%';
